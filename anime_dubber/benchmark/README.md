# reference.wav — placeholder

⚠️ **ЗАМЕНИ перед запуском бенчмарка!**

Текущий файл - 5 секунд тишины (заглушка для проверки пайплайна).

Для реального бенчмарка:
1. Запиши 3-10 сек чистой речи (без музыки, шума, overlap)
2. 24kHz mono WAV
3. Замени `benchmark/reference.wav`

Без этого все 3/5/10 строк в `prompts.json` дадут одинаковый пустой output (TTS не на чем клонировать).

См. `HUMAN_SCORE.md` для критериев оценки.
